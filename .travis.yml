language: ruby
rvm:
#  - 1.9.3
  - 2.0.0
  - 2.1.0
  - 2.2.0
  - jruby-9.0.1.0
  - jruby-head

# Workaround issue of jruby-head configuration on Travis CI 
matrix:
  allow_failures:
    - rvm: jruby-head

gemfile:
  - Gemfile
  
# whitelist
branches:  
  only:
    - master